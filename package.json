{
  "name": "streamnavigator",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check-prices": "node scripts/check-prices.js",
    "harness": "node scripts/audit-harness.js",
    "check": "npm run build-benchmarks && npm test && npm run harness",
    "build-benchmarks": "node scripts/build-benchmarks.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "pdfkit": "^0.20.2",
    "stripe": "^22.5.0"
  }
}
