node_modules/
.vercel
*.log

# Local patch files from earlier sessions — working notes, not source.
*.patch

# Claude Code local permission grants — machine-specific, not source.
.claude/settings.local.json
